{% extends 'layout.html.twig' %}
{% block content %}
    {% if docente.id %}
        <h1>Modificar docente: {{ docente.id }}</h1>
    {% else %}
        <h1>Crear nuevo docente</h1>
    {% endif %}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group row">
        {% if docente.id %}
                <div class="col-sm-2">
                    <a href="{{ path('docente_eliminar', {'id': docente.id}) }}">
                        <button type="button" class="btn btn-danger">Eliminar</button>
                    </a>
                </div>
        {% else %}
        {% endif %}
        <div class="col-sm-10">
            <a href="{{ path('docente_listar') }}" class="btn btn-info"><i class="fa fa-arrow-left"></i> Volver</a>
            <button type="submit" id="modificar_docente_enviar" name="guardar" class="btn-primary btn">
                <i class="fa fa-check"></i> Guardar
            </button>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}