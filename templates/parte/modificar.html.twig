{% extends 'layout.html.twig' %}
{% block title %}Modificar Parte{% endblock %}
{% block content %}
    {% if parte.id %}
        <h1>Modificar parte: {{ parte.id }}</h1>
    {% else %}
        <h1>Crear nuevo parte</h1>
    {% endif %}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group row">
        {% if parte.id %}
                <div class="col-sm-2">
                    <a href="{{ path('parte_eliminar', {'id': parte.id}) }}">
                        <button type="button" class="btn btn-danger">Eliminar</button>
                    </a>
                </div>
        {% else %}
        {% endif %}
        <div class="col-sm-10">
            <a href="{{ path('parte_listar') }}" class="btn btn-info"><i class="fa fa-arrow-left"></i> Volver</a>
            <button type="submit" id="modificar_parte_enviar" name="guardar" class="btn-primary btn">
                <i class="fa fa-check"></i> Guardar
            </button>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}