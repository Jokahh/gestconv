{% extends 'layout.html.twig' %}
{% block title %}Modificar Medida{% endblock %}
{% block content %}
    {% if medida.id %}
        <h1>Modificar medida: {{ medida.id }}</h1>
    {% else %}
        <h1>Crear nueva medida</h1>
    {% endif %}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group row">
        {% if medida.id %}
                <div class="col-sm-2">
                    <a href="{{ path('medida_eliminar', {'id': medida.id}) }}">
                        <button type="button" class="btn btn-danger">Eliminar</button>
                    </a>
                </div>
        {% else %}
        {% endif %}
        <div class="col-sm-10">
            <a href="{{ path('medida_listar') }}" class="btn btn-info"><i class="fa fa-arrow-left"></i> Volver</a>
            <button type="submit" id="modificar_medida_enviar" name="guardar" class="btn-primary btn">
                <i class="fa fa-check"></i> Guardar
            </button>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}