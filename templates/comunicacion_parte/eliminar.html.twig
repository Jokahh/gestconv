{% extends 'layout.html.twig' %}
{% block title %}Eliminar Comunicación de Parte{% endblock %}
{% block content %}
    <h1>Eliminar la comunicación de parte</h1>

    <div class="alert alert-danger">
        La comunicación de parte "{{ comunicacionParte.id }}" será eliminada de la base de datos.
        La operación no se puede deshacer.
    </div>
    <form method="post">
        <a href="{{ path('comunicacion_parte_listar') }}" class="btn btn-info">Volver al listado</a>
        <button type="submit" name="confirmar" class="btn btn-danger">Confirmar borrado</button>
    </form>
{% endblock %}