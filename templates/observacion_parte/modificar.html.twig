{% extends 'layout.html.twig' %}
{% block title %}Modificar Observación de Parte{% endblock %}
{% block content %}
    {% if observacionParte.id %}
        <h1>Modificar la observación de parte: {{ observacionParte.id }}</h1>
    {% else %}
        <h1>Crear nueva observación de parte</h1>
    {% endif %}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group row">
        {% if observacionParte.id %}
            <div class="col-sm-2">
                <a href="{{ path('observacion_parte_eliminar', {'id': observacionParte.id}) }}">
                    <button type="button" class="btn btn-danger">Eliminar</button>
                </a>
            </div>
        {% else %}
        {% endif %}
        <div class="col-sm-10">
            <a href="{{ path('observacion_parte_listar') }}" class="btn btn-info"><i class="fa fa-arrow-left"></i>
                Volver</a>
            <button type="submit" id="modificar_observacion_parte_enviar" name="guardar" class="btn-primary btn">
                <i class="fa fa-check"></i> Guardar
            </button>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}