{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Eliminar el tramo</h1>

    <div class="alert alert-danger">
        El tramo "{{ tramo.id }}" será eliminado de la base de datos.
        La operación no se puede deshacer.
    </div>
    <form method="post">
        <a href="{{ path('tramo_listar') }}" class="btn btn-info">Volver al listado</a>
        <button type="submit" name="confirmar" class="btn btn-danger">Confirmar borrado</button>
    </form>
{% endblock %}