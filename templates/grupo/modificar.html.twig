{% extends 'layout.html.twig' %}
{% block title %}Modificar Grupo{% endblock %}
{% block content %}
    {% if grupo.id %}
        <h1>Modificar grupo: {{ grupo.id }}</h1>
    {% else %}
        <h1>Crear nuevo grupo</h1>
    {% endif %}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group row">
        {% if grupo.id %}
                <div class="col-sm-2">
                    <a href="{{ path('grupo_eliminar', {'id': grupo.id}) }}">
                        <button type="button" class="btn btn-danger">Eliminar</button>
                    </a>
                </div>
        {% else %}
        {% endif %}
        <div class="col-sm-10">
            <a href="{{ path('grupo_listar') }}" class="btn btn-info"><i class="fa fa-arrow-left"></i> Volver</a>
            <button type="submit" id="modificar_grupo_enviar" name="guardar" class="btn-primary btn">
                <i class="fa fa-check"></i> Guardar
            </button>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}