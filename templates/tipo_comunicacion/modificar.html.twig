{% extends 'layout.html.twig' %}
{% block title %}Modificar Tipo de Comunicación{% endblock %}
{% block content %}
    <div class="row">
        <div class="col d-flex justify-content-center align-items-center border rounded bg-light mb-2 mx-3 p-2">
            {% if tipoComunicacion.id %}
                <h3>Modificar tipo de comunicación #{{ tipoComunicacion.id }}</h3>
            {% else %}
                <h3>Crear nuevo tipo de comunicación</h3>
            {% endif %}
        </div>
    </div>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group row">
        {% if tipoComunicacion.id %}
            <div class="col-sm-2">
                <a href="{{ path('tipo_comunicacion_eliminar', {'id': tipoComunicacion.id}) }}">
                    <button type="button" class="btn btn-danger"><i class="fa fa-trash"></i> Eliminar</button>
                </a>
            </div>
        {% else %}
        {% endif %}
        <div class="col-sm-10">
            <a href="{{ path('tipo_comunicacion_listar') }}" class="btn btn-info"><i class="fa fa-arrow-left"></i>
                Volver</a>
            <button type="submit" id="modificar_tipoComunicacion_enviar" name="guardar" class="btn-primary btn">
                <i class="fa fa-check"></i> Guardar
            </button>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}