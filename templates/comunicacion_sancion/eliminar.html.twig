{% extends 'layout.html.twig' %}
{% block title %}Eliminar Comunicación de Sanción{% endblock %}
{% block content %}
    <h1>Eliminar la comunicación de sanción</h1>

    <div class="alert alert-danger">
        La comunicación de sanción "{{ comunicacionSancion.id }}" será eliminada de la base de datos.
        La operación no se puede deshacer.
    </div>
    <form method="post">
        <a href="{{ path('comunicacion_sancion_listar') }}" class="btn btn-info">Volver al listado</a>
        <button type="submit" name="confirmar" class="btn btn-danger">Confirmar borrado</button>
    </form>
{% endblock %}